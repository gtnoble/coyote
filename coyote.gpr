with "config/coyote_config.gpr";
project Coyote is

   for Languages use ("Ada", "C");
   for Source_Dirs use ("src/", "src/llm/", "config/", "tools/");
   for Object_Dir use "obj/" & Coyote_Config.Build_Profile;
   for Create_Missing_Dirs use "True";
   for Exec_Dir use "bin";
   for Main use ("coyote.adb", "coyote_list_sessions.adb", "coyote_open.adb");


   package Compiler is
      for Default_Switches ("Ada") use Coyote_Config.Ada_Compiler_Switches;
      for Switches ("C") use ("-O2", "-I/usr/include/x86_64-linux-gnu");
   end Compiler;

   package Binder is
      for Switches ("Ada") use ("-Es"); --  Symbolic traceback
   end Binder;

   package Linker is
      for Switches ("Ada") use ("-lcurl", "-lncursesw");
   end Linker;

   package Install is
      for Artifacts (".") use ("share");
   end Install;

end Coyote;
